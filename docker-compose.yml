version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: bot-manutencao:latest
    env_file:
      - .env
    environment:
      DATABASE_URL: file:./data/dev.db
      NODE_ENV: production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
